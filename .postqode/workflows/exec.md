Isolate and execute the exact step specified by the user from `docs/PLAN.md`, cross-referencing the rules in `docs/PRD.md`.

You must write both the functional application code and the corresponding automated validation tests for this step.

Once written, execute the tests in the local terminal to verify the implementation completely satisfies the specification.